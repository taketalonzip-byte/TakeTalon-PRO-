const tours = [{ code: "PGA", slug: "pga", name: "PGA TOUR", country: "USA", emblem: "https://a.espncdn.com/i/leaguelogos/golf/500/pga.png" }, { code: "EUR", slug: "eur", name: "DP World Tour", country: "Europe", emblem: "https://a.espncdn.com/i/leaguelogos/golf/500/eur.png" }, { code: "LPGA", slug: "lpga", name: "LPGA Tour", country: "USA", emblem: "https://a.espncdn.com/i/leaguelogos/golf/500/lpga.png" }, { code: "LIV", slug: "liv", name: "LIV Golf", country: "International", emblem: "https://a.espncdn.com/i/leaguelogos/golf/500/liv.png" }, { code: "CHAMPIONS", slug: "champions-tour", name: "PGA TOUR Champions", country: "USA", emblem: "https://a.espncdn.com/i/leaguelogos/golf/500/champions-tour.png" }];
const headers = { Accept: "application/json", "User-Agent": "Mozilla/5.0 (compatible; TakeTalon/1.0)" };
const load = async () => { const groups = await Promise.all(tours.map(async (tour) => { try { const r = await fetch(`https://site.web.api.espn.com/apis/site/v2/sports/golf/${tour.slug}/scoreboard`, { headers, signal: AbortSignal.timeout(10000) }); if (!r.ok) return []; const d: any = await r.json(); return (d.events || []).map((event: any) => { const comp = event.competitions?.[0] || {}; const type = comp.status?.type || event.status?.type || {}; const state = type.state; const status = state === "in" ? "IN_PLAY" : state === "post" ? "FINISHED" : "SCHEDULED"; const players = (comp.competitors || []).slice(0, 50).map((c: any, i: number) => ({ id: String(c.id || c.athlete?.id || i), name: c.athlete?.displayName || c.athlete?.fullName || `Player ${i + 1}`, shortName: c.athlete?.shortName || c.athlete?.displayName || `P${i + 1}`, position: Number(c.order || i + 1), score: c.score != null ? String(c.score) : "E", odds: Number((3 + (i + 1) * 1.6).toFixed(2)) })); return { id: String(event.id), sport: "golf", tour: { code: tour.code, name: tour.name, country: tour.country, emblem: tour.emblem }, name: event.name || tour.name, shortName: event.shortName || event.name || tour.name, startDate: comp.startDate || event.date || new Date().toISOString(), endDate: event.endDate || comp.endDate, status, shortDetail: type.shortDetail, round: Number(comp.status?.period || event.status?.period || 0), isLive: status === "IN_PLAY", course: comp.course?.name || comp.venue?.fullName, broadcast: comp.broadcast, leaderboard: players }; }).filter(Boolean); } catch { return []; } })); return groups.flat(); };
export const onRequest = async () => { const tournaments = await load(); return Response.json({ ok: true, sport: "golf", provider: "espn", count: tournaments.length, tournaments }); };
export { load };
